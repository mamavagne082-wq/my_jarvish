{
  "name": "jarvis-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix Jarvis/agent-starter-react",
    "next:dev": "npm run next:dev --prefix Jarvis/agent-starter-react",
    "backend": "npm run backend --prefix Jarvis/agent-starter-react",
    "build:exe": "call Build_Jarvis_EXE.bat",
    "build:apk": "call Build_Mobile_APK.bat",
    "build:all": "call Build_All.bat"
  },
  "dependencies": {
    "livekit-client": "^2.22.3"
  }
}
